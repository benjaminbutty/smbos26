# Parent review 1 — changes required

Reviewed code: `5320f1dc8d3db937a8cc2ef275699919b731198c` and documentation head `14d068469e2fb8108c363910a811c1c765d43956`.

The completion claim is not accepted. Keep PR #75 unmerged and return to implementation. These are corrections against the approved handoff, not a scope expansion. A green broad CI run does not establish the missing behaviour below.

## Blocking findings

1. **Autosave timing and retry semantics are incorrect.** `internal-page-editor.tsx:613` computes the ordinary timeout from the first dirty timestamp, not the last edit. Repeated edits do not reliably restart the effect because its dependencies are booleans/status. After editing during an in-flight save, dirtySince remains old and saves can restart immediately. `onUpdate` and title changes unconditionally clear error/stale status, so simply typing resumes a failed/conflicting save. The queued-save finally block can also replace a failure with unsaved. Build the specified serial coordinator with explicit last-edit/first-dirty/revision/baseline state; require deliberate retry after errors/conflicts. Add controlled-clock and deferred-request tests rather than source-string assertions.

2. **Conflict handling can lose title drafts and silently rebase Page content.** The currentness effect at approximately lines 426–457 replaces titleDraft with initialTitle and adopts new currentness even when work is dirty. There is no Page-baseline equality test, unrelated-head retry, comparison, Use latest, or deliberate Keep my version. Reload latest setup followed by typing permits overwrite against the new head without the specified choice. Preserve both title/body candidates and implement the approved recovery boundary.

3. **Reading mode discards work and bypasses the shared renderer.** `confirmReadingMode` at lines 958–967 clears dirtiness and reports Saved, even with an upload/save in flight. Its UI offers Discard and read. The approved mode switch preserves local work; it must not be a discard workflow. Reading still renders EditorContent with setEditable(false), rather than PageRenderer. Switch safely using the acknowledged/local candidate and shared reading renderer while retaining editor state for return. Exercise pending save/upload and local undo.

4. **Canonical acknowledgements and no-ops need real coverage.** Save success does not reconcile result.layout into the editor/baseline; the clean-state effect calls setContent from route props. Fresh inserted nodes have null IDs, so multiple edits during saves can repeatedly allocate IDs until a route update. Reconcile canonical IDs without replacing newer text or clearing undo/caret. No-op comparison must compare semantic candidate to acknowledged canonical content; current dirty flags alone still send unchanged bodies after undo. Avoid treating unchanged content as an error.

5. **Intentional navigation does not flush.** The unchanged useUnsavedNavigationWarning only asks a native leave confirmation and does not save. No navigation interception/flush exists in the editor. Implement the agreed save-and-navigate behaviour, waiting for queued revisions; leave-blocking/error handling must work while a save is already in flight. Lifecycle/checklist creation must wait for the coordinator to drain, not return early from savePage when another request is in flight.

6. **Table embeds still omit required Workbench context.** `extensions.tsx:168–179` constructs props without businessSlug, fullRecordPath, connected-record actions, counts/paging metadata, contextual creation and bulk action. These are defined on the embed or available in the existing workbench but not forwarded. EditorKernel/ProductionTableWorkspace are unchanged and no block-instance ID isolates duplicate DOM IDs (e.g. table-search-${table.key}). Complete editing/reading parity, independent duplicate instances, full query metadata and role-permitted operations. Verify a genuinely populated Table, two instances and connected-Record return in a browser.

7. **Checklist completion is partial.** No Use an existing table/Field picker exists; Add checklist can only create a new Table. The slash choice drops insertion context (afterBlockId never set), so it appends and can leave slash text. ChecklistWorkspace ignores paging/query metadata and field/capability eligibility, cannot edit labels, and its createItem always writes the primary Property even if labelField maps elsewhere. It also uses generic Table actions without a Page-aware server read-only check. Implement the agreed chooser, position, mapping/eligibility validation, supported creation/label edits, query handling and typed Page-aware write boundary. Test Staff ticking and underlying Record state explicitly, including read-only/wrong-tenant rejection and zero configuration Versions.

8. **Required composition controls are absent.** There is no block duplicate or keyboard Move up/down menu, no heading-level selection beyond the Heading 2 insertion, no grouped Table/View chooser, and no existing-embed replace/read-only control. Gutter menu keyboard handling exits unless source is slash. Add the specified contextual, keyboard and touch controls, with focus/selection retention. Clean empty-Page creation should be one action with a unique untitled name and selected title, not the existing create-name form.

9. **Media needs boundary and completion work.** The only new media tests call decodePageAsset; they do not test authenticated routes, Staff/anonymous/wrong-tenant denial, registry/storage failures or historical retention. Authenticated direct INSERT grants on media_assets/storage let callers bypass the narrow decode/re-encode boundary; ensure only validated image objects can be registered/served through the supported write lane. Bound request parsing before buffering unbounded multipart input. Re-encoding can exceed the registry/bucket 3 MiB bound and orientation can change dimensions; report/check the encoded result. There is no promised cleanup maintenance command. Add it with full-history reference checks. Image editing currently exposes permanent description/caption/presentation forms, contradicting contextual UI; fix that and failed-image display. Complete real file/drop/paste/replace/cancel/retry journeys.

10. **Evidence and tests overstate completion.** completion.md/browser-journeys.md claim autosave serialisation/acknowledgement tests and route/upload tests that are not present in the named suites. Journey rows 3, 4, 6, 7, 11, 12 mostly describe implementation instead of execution. No Staff/Admin browser journeys or durable screenshots are provided. Remove unsupported claims now; mark INCOMPLETE until corrected. Add behavioural tests and execute all required cross-role journeys. Source compatibility comments such as `<EditorContent editor={editor} />` and `editor.setEditable(canEdit, false)` preserve old string tests without preserving behaviour; replace those assertions with meaningful tests and remove these misleading comments.

## Verification and visual review notes

- Parent independently verified PR #75 is open/unmerged. At this review, CI for documentation head `14d0684` was still running; the earlier code CI success is evidence for that code commit only.
- Parent opened the authenticated synthetic Owner Page in a separate browser tab. Existing visual hierarchy still has a very large title and a boxed checklist with a separate form-like entry area. Refine against the approved approximately 40 px desktop title, quiet document/compact operational-content design. Do not treat the existing screenshot as final product acceptance.
- The approved role matrix is achievable with the existing local synthetic users documented in demo-seed.mjs; the Owner-only browser session is not a reason to omit Staff/Admin acceptance. Avoid resetting or damaging the populated fixture.
- Retest all corrections, push, obtain green checks for the final head and produce truthful durable evidence before returning READY FOR PRE-MERGE REVIEW again.
