# ADR-032 — Builder-assisted generic Record creation

Status: Phase 12A is complete and merged through PR #17.

## Decision

Builder-assisted creation of one generic graph Record uses the existing graph
Object/Field model and the ordinary Record insert trigger. A ready one-step
`create_record` plan is converted into a strict, transient typed intent. The
server validates the target Object, eligible writable Fields and typed values,
reads authoritative defaults, and requires explicit Owner/Admin confirmation.

The confirmation token is a short-lived HMAC envelope bound to the
authenticated Business and actor. The final action rechecks the exact
Object/schema/state currentness, then calls one narrow
`create_confirmed_graph_record` RPC. PostgreSQL locks the Business head for
share, the Object definition for update, and inserts the Record atomically;
existing validation triggers remain authoritative. A stale or replayed token
fails closed without a second insert.

The model receives the unchanged AI-safe Business context and ready plan. It
may return only exact existing Object and Field keys present in the supplied
configuration context. It may not invent new keys, UUIDs, IDs, defaults,
Records, relationships or mutation authority. The default and production
mappings remain disabled; the Terra policy and live
qualification/reliability harness are engineering-only and separately gated.

Eligibility requires an existing active target Object with at least one active
non-file writable Field, no active required incoming Relationship that makes
standalone creation incomplete, no active preorder Order or Order Item Object
mapping, and no required File Field without a usable default.

## Invalid-request qualification evidence

The first Phase 12A qualification run against exact SHA
`7c03b743480016f24c5d6922ec6e4933103dce35` stopped on
`product_text_currency_default`, repetition 1, with `passed: false`,
`output_state: null`, `field_value_count: 0`, `failure_class:
provider_execution`, `failed_gate_codes: [provider_execution]`, one attempt,
incomplete usage, zero input/output tokens, zero estimated microusd,
`elapsed_ms: 1061`, `error_code: ai_execution_failed` and
`validation_reason_code: null`. The qualification aggregate was 8 total
scenarios, 0 passed, 1 failed, 1 attempt, zero input/output tokens, zero
estimated microusd, 1061 ms and exit code 1. No model output was returned or
evaluated.

The existing redaction prevented exact invalid-request stage classification.
The cause is intentionally not guessed: local `OpenAiSchemaAdaptationError`
and provider HTTP 400/422 failures were both mapped to public
`StructuredAiProviderError(kind: invalid_request)`. The bounded correction
adds only the finite internal stages `local_schema_adaptation`,
`provider_schema_rejected`, `provider_response_format_rejected`,
`provider_model_rejected`, `provider_parameter_rejected` and
`provider_invalid_request_unknown`. A depth- and cycle-bounded exact-class
cause traversal carries the code only to the engineering evaluation report as
`provider_reason_code`; public AI errors and Business audit metadata remain
unchanged and raw provider material is never emitted.

Deterministic transport coverage proves that the exact registered Record
output schema succeeds through draft-7 JSON Schema generation, the OpenAI
adapter and an injected Responses client, which receives one valid request.
Reliability was not run, qualification was not rerun, production remains
disabled, and no additional live provider spend occurred.

The operator subsequently ran the same first qualification scenario against
exact head `a6054b63477664cc9e422e4c2cb6e8179dcddd84`. It again stopped before
model output, with `passed: false`, `output_state: null`,
`field_value_count: 0`, `failure_class: provider_execution`, one attempt,
`usage_complete: false`, zero input/output tokens, zero estimated microusd,
`elapsed_ms: 715`, `error_code: ai_execution_failed`,
`validation_reason_code: null` and `provider_reason_code:
provider_schema_rejected`. The aggregate was 8 total scenarios, 0 passed, 1
failed, 1 attempt, zero tokens, zero estimated microusd, 715 ms and exit code
1. No model output or scenario quality was produced or evaluated.

This is evidence-supported transport diagnosis: the Record URL variant had
been built with `z.string().url()`, whose draft-7 JSON Schema emitted
`format: "uri"`; the documented OpenAI Structured Outputs format subset does
not include `uri`. The correction replaces that URL transport shape with a
bounded structural string (`minLength: 1`, `maxLength: 2048`, HTTP(S) prefix
pattern) and keeps URL syntax and exact `http:`/`https:` protocol validation
in the shared deterministic runtime schema. The OpenAI adapter now explicitly
allows only `date-time`, `time`, `date`, `duration`, `email`, `hostname`,
`ipv4`, `ipv6` and `uuid`, and fails unsupported formats locally. Reliability
was not run, production remains disabled, and no further live spend occurred.

The operator then reran qualification against exact SHA
`c6e4cf6e72d0a59231eb729489319de2c6a62b0b`. The same first scenario,
`product_text_currency_default`, repetition 1, stopped before model output
with `passed: false`, `output_state: null`, `field_value_count: 0`,
`failure_class: provider_execution`, `failed_gate_codes:
[provider_execution]`, one attempt, incomplete usage, zero input/output
tokens, zero estimated microusd, `elapsed_ms: 3507`, `error_code:
ai_execution_failed`, `validation_reason_code: null` and
`provider_reason_code: provider_schema_rejected`. The aggregate was 8 total
scenarios, 0 passed, 1 failed, 1 attempt, zero tokens, zero estimated cost,
3507 ms and exit code 1. No model output or scenario quality was evaluated.

The URL correction did not resolve the complete Record-schema rejection. No
further individual keyword, branch, union or wrapper construct is claimed as
the cause. Ordinary qualification is paused until one engineering-only
compatibility run isolates the rejected construct and the exact full schema
passes compatibility. Reliability was not run, the production Record-intent
mapping remains disabled, and this diagnostic implementation makes no live
provider call.

The compatibility gate uses the exact OpenAI Responses provider construction,
`gpt-5.6-terra`, medium reasoning, strict `text.format`, `store: false`, one
attempt and a fixed synthetic `Return the smallest valid result.` input. It
never receives an owner request, Business context, Object or Field values,
operational Records, tenant/actor IDs or real PII. Activation requires all of
`RUN_LIVE_OPENAI_RECORD_CREATION_SCHEMA_COMPATIBILITY=1`,
`AI_PROVIDER=openai` and a nonblank `OPENAI_API_KEY`; a key alone grants no
permission.

The frozen base matrix contains 20 ordered probes: a strict transport
baseline; the ready/clarification state union without Field values; one
short-text ready branch; individual long-text, email, phone and URL branches
plus their cumulative text-like union; individual number, currency, boolean,
date and datetime branches plus their cumulative primitive-value union;
individual select, status and multi-select branches plus their cumulative
option union; the complete 13-branch ready Field union; and the exact
registered `builderRecordCreationIntentOutputSchema`. Schema rejection does
not stop later schema probes. Authentication failure, outage, rate limiting,
timeout or an unexpected non-schema failure stops immediately.

If every individual branch passes but the complete union fails, at most five
additional probes binary-search the smallest failing ordered branch count and
test a rotated same-size branch set to distinguish branch-count evidence from
a specific combination/order. If the complete ready union passes but the
exact schema fails, at most four probes isolate the clarification branch,
source-step constraints, annotations and rebuilt ready/clarification
composition while the already-passed baseline holds the outer `result`
wrapper constant. The first structural failure then receives seven
engineering-only remove-one-keyword-family variants: string bounds, patterns,
formats, numeric bounds, array bounds, annotations, and safely inlineable
`$defs`/`$ref` reuse. These variants never replace the production contract.

The command permits at most 32 provider attempts. Each probe reserves 16,000
input tokens and 128 output tokens, exactly 41,920 microusd. The aggregate
reservation is 1,341,440 microusd under a hard ceiling of 1,350,000 microusd,
materially below the 4,183,040-microusd eight-scenario qualification
reservation. Reports contain only a canonical schema digest, deterministic
schema metrics, finite result/provider classifications, allow-listed schema
keyword/path context, attempts, usage, cost and elapsed time. They never emit
schema JSON, prompts, inputs, model output, raw provider messages/parameters,
headers, bodies or credentials.

The installed OpenAI SDK comparison generates a strict
`{ result: builderRecordCreationIntentOutputSchema }` schema with
`zodTextFormat` when supported and compares it with the SMBOS adapted schema.
It emits only helper-generation success, canonical digests, metrics and finite
keyword/path difference categories; neither schema is printed, and production
is not switched to the helper.

The operator ran the bounded compatibility diagnostic against exact SHA
`356b5fd237a7f821cda4744a7b51a7a0fb45e4b7`. It completed normally after 27
probes: 17 were accepted, 10 were rejected, the exact schema was rejected and
the first structural failure was `d_email` in the `text_like` family with
conclusion `individual_branch_rejected`. The individual `d_email` probe was
rejected; `keyword_without_patterns` was accepted; and
`keyword_without_formats` remained rejected. All non-email individual Field
branches passed, as did the primitive and option cumulative families. The
complete Field union and exact Record schema failed because they contained the
email branch. No model business-quality evaluation occurred.

The bounded correction replaces both duplicated Record email definitions with
one server-owned `recordCreationEmailValueSchema`. Its generated provider shape
is exactly a string with `minLength: 1` and `maxLength: 320`; it has no email
format or pattern. A deterministic runtime refinement delegates to Zod's strict
email parser, and the same schema is used by the model-intent and trusted
creation/confirmation boundaries. Valid email strings are neither trimmed,
normalised nor otherwise rewritten, and semantic validation requires the exact
model-returned email string to occur in the owner request. At the time of this
historical correction record, compatibility and ordinary qualification had not
yet been rerun; the accepted final evidence is recorded below. The failed runs
remain historical evidence and do not change the frozen subject.

## Accepted Phase 12A enablement evidence

The frozen Record-intent subject and its previously qualified provider
contract were accepted against exact head SHA
`99988cc7950bb009f290f9f23f84f61dbbef4d0e`:

- Compatibility completed with 20/20 probes accepted, 0 rejected, the exact
  full Record schema accepted, and exit code 0. Reported cost was 29,976
  microusd.
- Qualification completed with 8/8 scenarios passed, 0 failed, 8 attempts,
  17,265 input tokens, 937 output tokens, 57,220 microusd and exit code 0.
- Reliability completed with 24/24 executions passed across 8/8 scenarios,
  3 repetitions each, 24 attempts, 51,795 input tokens, 2,831 output tokens,
  171,960 microusd and exit code 0.

These reports contained no failure, error, validation or provider reason codes.
The private authenticated Builder OpenAI runtime now maps the frozen Record
task clone to `builder_record_creation_intent_terra_medium_v1` and the
`openAiBuilderRecordCreationIntentPolicy`. The global/default production AI
runtime, including its OpenAI mode, still maps the registered task to
`builder_record_creation_intent_disabled_v1` and does not register the Terra
policy. Disabled Builder mode remains disabled. At Phase 12A, the private
runtime contained exactly five tasks and five policies; Phase 12B adds its
sixth update task separately.

Planning remains the only entry to intent generation, and only a ready
one-step `create_initial_record` plan can reach the Record path. Unsupported or
mixed plans remain bounded without intent generation. Final confirmation is
deterministic and AI-free: it performs no provider call, task/accounting
reservation, planning, configuration mutation or provider-version change.
PR #17 merged the Phase 12A implementation. Phase 12B remains independently
reviewable on its feature branch.

## Consequences

Product-specific creation is not added to the platform. Generic concepts such
as Equipment and Catering Enquiry use the same Object/Field/Record path, with
no new table, primitive, queue, receipt table or configuration version. The
existing runtime reads the newly created Record through the selected internal
View. Preparation and confirmation are deterministic after the intent task;
the final action performs no AI call, accounting reservation, configuration
mutation or provider call.

The state RPC and confirmed-create RPC are the new database boundary. They
return only the bounded state/result needed by the server, enforce tenant and
capability checks, and use PII-free schema/state digests for currentness. The
implementation is intentionally limited to one generic Record and does not
add Record update/delete, relationship creation, file upload, public form
changes, or generic operational action dispatch.
