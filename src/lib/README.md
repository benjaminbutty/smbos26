# Shared library boundary

Small, application-wide utilities may live here when they have more than one
real consumer. Provider-specific or domain-specific code should remain in its
own documented boundary.
